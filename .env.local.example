# Where the Scout backend (scout/) is listening.
# The cockpit probes this; unreachable just means it runs in SIM mode.
SCOUT_API_URL=http://localhost:8000

# Agent id registered on AgentOS.
SCOUT_AGENT_ID=scout
